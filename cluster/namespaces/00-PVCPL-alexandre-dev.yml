kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: alexandre-dev-pipeline-claim
  namespace: alexandre-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

