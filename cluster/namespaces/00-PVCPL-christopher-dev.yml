kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: christopher-dev-pipeline-claim
  namespace: christopher-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

