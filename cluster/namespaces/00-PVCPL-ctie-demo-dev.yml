kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ctie-demo-dev-pipeline-claim
  namespace: ctie-demo-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

